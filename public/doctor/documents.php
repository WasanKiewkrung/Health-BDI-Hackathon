<?php require_once __DIR__.'/../../backend/shared/auth.php'; require_role('doctor'); require_once __DIR__.'/../../backend/shared/layout.php';
$hn=$_GET['hn']??$_POST['hn']??''; $p=null; if($hn){$st=db()->prepare('SELECT * FROM patients WHERE hn=?');$st->execute([$hn]);$p=$st->fetch();}
if($_SERVER['REQUEST_METHOD']==='POST' && $p){$st=db()->prepare('INSERT INTO documents(patient_id,visit_id,doc_type,title,department_to,doctor_to,content,created_by) VALUES(?,?,?,?,?,?,?,?)');$st->execute([$p['id'],$_POST['visit_id']?:null,$_POST['doc_type'],$_POST['title'],$_POST['department_to'],$_POST['doctor_to'],$_POST['content'],$_SESSION['user']['id']]); header('Location: document-view.php?id='.db()->lastInsertId()); exit;}
$visits=[]; if($p){$v=db()->prepare('SELECT id,visit_date,department,chief_complaint FROM visits WHERE patient_id=? ORDER BY visit_date DESC');$v->execute([$p['id']]);$visits=$v->fetchAll();}
app_header('เอกสาร / PDF','doctor','docs'); ?><a class="back" href="<?= $p?'patient-profile.php?hn='.urlencode($p['hn']):'dashboard.php' ?>">← กลับ</a><div class="card"><h3>สร้างเอกสาร PDF</h3><form method="get"><div class="row"><div class="field"><label>HN</label><input name="hn" value="<?=e($hn)?>" placeholder="HN000001"></div><div class="field"><button class="btn secondary">โหลดข้อมูล</button></div></div></form><?php if($p):?><form method="post" class="form-stack"><input type="hidden" name="hn" value="<?=e($p['hn'])?>"><div class="row"><div class="field"><label>ผู้ป่วย</label><input value="<?=e($p['hn'].' '.$p['first_name'].' '.$p['last_name'])?>" disabled></div><div class="field"><label>อ้างอิง Visit</label><select name="visit_id"><option value="">ไม่ระบุ</option><?php foreach($visits as $v):?><option value="<?=$v['id']?>"><?=e($v['visit_date'].' '.$v['department'].' '.$v['chief_complaint'])?></option><?php endforeach;?></select></div><div class="field"><label>Template</label><select name="doc_type"><option value="medical_certificate">ใบรับรองแพทย์</option><option value="symptom_report">รายงานอาการ</option><option value="lab_request">ใบส่งตรวจ Lab</option><option value="referral">ใบส่งต่อ / Consult</option><option value="operation_report">รายงานผ่าตัด / วิสัญญี</option></select></div><div class="field"><label>ชื่อเอกสาร</label><input name="title" value="เอกสารทางการแพทย์ USE MED"></div><div class="field"><label>ส่งต่อไปแผนก</label><select name="department_to"><option></option><option>อายุรกรรม</option><option>ศัลยกรรมทั่วไป</option><option>วิสัญญี</option><option>สูติ-นรีเวช</option><option>ICU</option><option>Lab</option><option>Radiology</option></select></div><div class="field"><label>ชื่อแพทย์ปลายทาง</label><input name="doctor_to" placeholder="พิมพ์ชื่อแพทย์ที่ส่งตัวไป"></div></div><div class="field"><label>เนื้อหา</label><textarea name="content" required>สรุปอาการ ผลตรวจ การรักษา และคำแนะนำ...</textarea></div><button class="btn">สร้างเอกสาร + เปิดหน้า PDF</button></form><?php endif;?></div><?php app_footer();?>
